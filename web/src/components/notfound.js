/* eslint-disable */
import { Container } from "react-bootstrap";
import notfound_alarm from "./alarm/notfound_alarm";

const NotFound = () => {
    <Container className={"text-center"} fluid></Container>;
    notfound_alarm();
};

export default NotFound;
